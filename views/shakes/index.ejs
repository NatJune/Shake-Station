<%- include('../partials/header') %>

    <table id="list">
        <thead>
            <tr>
            <th>product type</th>
            <th>flavor</th>
            <th>calories</th>
            <th>price</th>
            <th>quality</th>
            </tr>
        </thead>
        <tbody>
            <% shakes.forEach(function(s) { %>
                <tr>
                    <td><%=s.productType%></td>
                    <td><%=s.flavor %></td>
                    <td><%=s.calories %></td>
                    <td><a href="/shakes/<%= s._id %>">DETAILS</a></td>
                </tr>
            <% }); %>
            <li>
                <form action="/shakes/<%= shakes %>?_method=DELETE" method="POST">
                    <button type="submit">🗑</button>
                </form>
                <a href="/shakes/<%= shakes %>">
                    <%= shakes.text %>
                    -
                    <%= shakes.done ? 'done' : 'not done' %>
                </a>
            </li>
        </tbody>
    </table>
 <%- include('../partials/footer') %>